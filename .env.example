# Database (PostgreSQL — User, Shipment, Wallet, Transaction)
DATABASE_URL="postgresql://user:password@localhost:5432/dmx?schema=public"

# MongoDB (Merchant module — KYC, booking, shipments)
MONGODB_URI="mongodb+srv://user:pass@cluster.mongodb.net/dmx"

# Auth
JWT_SECRET="dmx-logistics-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
ADMIN_EMAIL="admin@dmx.com"

# Payment (Paystack wallet funding)
PAYSTACK_SECRET_KEY="sk_test_xxx"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email (optional)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@dmx-logistics.com"

# Optional integrations
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""
# DOJAH_KEY=""   # KYC
# TERMII_API=""  # SMS

# Production: disable demo login (admin@dmx.com / password123)
# DISABLE_DEMO=true
